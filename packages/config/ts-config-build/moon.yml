$schema: https://moonrepo.dev/schemas/project.json
layer: configuration
language: typescript
tags:
  - tsconfig
  - tenant-shared
dependsOn: []
fileGroups:
  sources:
    - src/**/*
  tests:
    - src/**/*.test.*
    - src/**/*.spec.*
    - test/**/*
  configs:
    - "*.config.*"
    - tsconfig*.json
    - .eslintrc*
    - .prettierrc*
    - package.json
tasks:
  build:
    outputs: []
    deps:
      - ts-config-base:build
      - ^:build
