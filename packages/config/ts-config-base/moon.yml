$schema: https://moonrepo.dev/schemas/project.json
layer: configuration
language: typescript
tags:
  - tsconfig
  - tenant-shared
fileGroups:
  sources:
    - src/**/*
  tests:
    - src/**/*.test.*
    - src/**/*.spec.*
    - test/**/*
  configs:
    - "*.config.*"
    - tsconfig*.json
    - .eslintrc*
    - .prettierrc*
    - package.json
tasks:
  build:
    command:
      - "npm"
      - "run"
      - "build"
    inputs:
      - "@globs(sources)"
      - "@globs(configs)"
    outputs: []
    options:
      cache: true
