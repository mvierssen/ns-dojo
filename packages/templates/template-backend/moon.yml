$schema: https://moonrepo.dev/schemas/project.json
type: application
language: typescript
tags:
  - tenant-shared
dependsOn: []
fileGroups:
  sources:
    - src/**/*
  tests:
    - src/**/*.test.*
    - src/**/*.spec.*
    - test/**/*
  configs:
    - "*.config.*"
    - tsconfig*.json
    - .eslintrc*
    - .prettierrc*
    - package.json
tasks:
  build:
    deps:
      - ^:build
      - eslint-config-node:build
      - ts-config-node:build
      - vitest-config-node:build
