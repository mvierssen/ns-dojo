$schema: https://moonrepo.dev/schemas/project.json
type: application
tags:
  - tenant-shared
  - vite
language: typescript
dependsOn: []
fileGroups:
  sources:
    - src/**/*
  tests:
    - src/**/*.test.*
    - src/**/*.spec.*
    - test/**/*
  configs:
    - "*.config.*"
    - tsconfig*.json
    - .eslintrc*
    - .prettierrc*
    - package.json
tasks:
  build:
    deps:
      - eslint-config-react:build
      - ts-config-react:build
      - vite-config-react:build
      - vitest-config-react:build
      - prettier-config:build
      - ^:build
